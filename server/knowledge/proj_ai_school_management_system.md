# AI School LMS (synthesized from manifest)
Source: /Users/yahya/Documents/AI School Management System/package.json

Synthesized from manifest, no README found.

Project name: `ai-school-lms` (version 0.1.0, private). A Next.js 14 application using TypeScript and Tailwind CSS, with Supabase as backend and OpenAI integration for AI features. Schema validation via Zod.

Scripts: `dev` (next dev), `build` (next build), `start` (next start), `typecheck` (tsc --noEmit).

Key dependencies:
- next ^14.2.13
- react / react-dom ^18.3.1
- @supabase/ssr ^0.5.0 and @supabase/supabase-js ^2.45.0 (auth, database)
- openai ^4.56.0 (AI features)
- zod ^3.23.8 (schema validation)

Dev dependencies: TypeScript ^5.6.2, Tailwind CSS ^3.4.13, PostCSS, autoprefixer, type definitions for Node and React.
